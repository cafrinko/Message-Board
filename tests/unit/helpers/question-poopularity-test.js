import { questionPoopularity } from 'message-board/helpers/question-poopularity';
import { module, test } from 'qunit';

module('Unit | Helper | question poopularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionPoopularity([42]);
  assert.ok(result);
});
