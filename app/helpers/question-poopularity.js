import Ember from 'ember';

export function questionPoopularity(params/*, hash*/) {
  var question = params[0];

//count the number of answers the question has
//put a glyphicon next to the number of questions with the most answers and the least answers
  //sort in descending order by the number of answers a question has
  //put a fire glyphicon by the question in the 0th array position
  //put a dust glyphicon by the question in the .length array position

}

export default Ember.Helper.helper(questionPoopularity);
